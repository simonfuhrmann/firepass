{
  "name": "firepass",
  "version": "1.2.0",
  "description": "Firepass - Password manager",
  "author": "Simon Fuhrmann",
  "license": "BSD-3-Clause",
  "repository": "github:simonfuhrmann/firepass",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -f static/index.mjs",
    "bundle": "esbuild src/index.ts --bundle --minify --target=es2020 --outfile=static/index.mjs",
    "watch": "npm run bundle -- --watch --servedir=static/"
  },
  "dependencies": {
    "firebase": "^12.0.0",
    "lit": "^3.0.0",
    "oxygen-mdc": "^1.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.25",
    "typescript": "^5.8.3"
  }
}
